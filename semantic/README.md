# there are semantic files, build yours custom semantic UI


``` bash
# install dependencies
npm install

# build for production with minification
gulp build

# semanticDist is a dist catalog
eg: 
<style src='./semantic/semanticDist/semantic.min.css'>
</style>